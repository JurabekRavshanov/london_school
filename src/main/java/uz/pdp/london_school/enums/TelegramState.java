package uz.pdp.london_school.enums;

public enum TelegramState {
    SELECT_LANG,
    SHARE_CONTACT,
    SELECT_COMMAND,
    SELECT_COURSE,
    SEE_PERSONAL_COURSES,
    SELECT_COURSE_TYPE,
    SELECT_TIMETABLE,
    SELECT_VISIT_DAY, START
}
